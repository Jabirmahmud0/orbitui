export default function App() {
  return (
    <main className="orbit-app-shell">
      <section className="orbit-hero">
        <p className="orbit-kicker">OrbitUI</p>
        <h1>Enterprise React component infrastructure, starting from a clean foundation.</h1>
        <p className="orbit-copy">
          This preview app exists to validate the build pipeline while the component library is assembled
          task by task.
        </p>
      </section>
    </main>
  );
}
